{
  "name": "lumi",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "dev": "turbo run dev --filter=./apps/*",
    "build": "turbo run build",
    "test": "pnpm run workspace:check && turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "workspace:check": "node scripts/workspace-graph.mjs"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
